import React from 'react';
import './NavBar.css';

const NavBar = (props) => (
	<header>
		<nav>
			<div className = "logo">Country Flag Guessing Game </div>
		</nav>
	</header>
);

export default NavBar;